name: Push Docker Container Beta

on:
  push:
    tags:
      - "v*"

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Read version from package.json
        id: get_version
        run: |
          version=$(jq -r .version package.json)
          echo "Version read from package.json: $version"
          echo "version=$version" >> $GITHUB_OUTPUT
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Promote alpha to beta
        id: promote-alpha-to-beta
        run: |
          docker pull soosio/dast:alpha
          docker tag soosio/dast:alpha soosio/dast:beta
          docker push soosio/dast:beta
          docker tag soosio/dast:alpha soosio/dast:v${{ steps.get_version.outputs.version }}
          docker push soosio/dast:v${{ steps.get_version.outputs.version }}

